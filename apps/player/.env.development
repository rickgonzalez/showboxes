# Player dev: leave VITE_SERVER_URL empty so pipeline/api.ts builds
# relative URLs. vite.config.ts's server.proxy forwards /api, /login,
# and /_next to the Next server on :3001, which makes the browser see
# everything as same-origin so cs_session rides along with requests.
VITE_SERVER_URL=""
